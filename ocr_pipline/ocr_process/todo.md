# todo list

+ [x] 修改架构，把所有ocr流程封装到ocr_process.cpp里的一个函数里，main里直接调用这个函数，直接输入几个模型和一个图片path，完成ocr动作
+ [ ] 添加 --folder 参数，指定要处理的文件夹路径
    + 有 --folder 参数时，自动忽略 --image 参数，没有 --folder 参数时，才处理 --image 参数指定的一张图片文件，两个都没有时候，自动打印用法说明退出
    + 该文件夹内容格式如下：
        root@nanoagent-79d6:/var/lib/openchronicle/screenshots# ls
        2026-05-15  # 可能还有其他文件夹
        root@nanoagent-79d6:/var/lib/openchronicle/screenshots# ls 2026-05-15/
        2026-05-15T16-18-17+08-00.jpg  2026-05-15T16-51-51+08-00.jpg
        2026-05-15T16-50-58+08-00.jpg  2026-05-15T16-58-13+08-00.jpg
        2026-05-15T16-51-40+08-00.jpg
    + 需要递归处理里面所有图片文件
    + 永远都是处理时间最新的图片（先遍历最新的文件夹里的图片，图片都带上"_ocr"后缀后再检索第二新的文件夹里的图片）
    + 每一个图片处理完，重命名这个图片，在原有文件名最后添加"_ocr"
    + 看到"_ocr"的图片，就不处理了
    + 处理完所有的图片，不要退出，每隔5s自动检查一次是否有新文件，直到用户输入ctrl+c，主动退出
    + 做两个函数：函数一：按照上面的需求获取一个最新的没有ocr过的图片文件，函数二：重命名这个图片，在原有文件名最后添加"_ocr"
    + 使用简单的循环结构调用上面两个函数完成这个功能
+ [ ] 不要保存json，而是直接把字符串上传
+ [ ] 添加 --log 参数，才打开log功能
+ [ ] 添加 --time 参数，才打开计时功能
+ [ ] 添加 --mem 参数，才打开内存监控功能